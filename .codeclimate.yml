version: '2'
exclude_patterns:
  - '**/*spec.ts'
plugins:
  duplication:
    enabled: true
    config:
      languages:
        typescript:
          similar-code:
            config:
              threshold: 95
